/* ============================================
   HELP OVERLAY
   Help button logic is in app.js (topbar)
   This module exists for consistency
   ============================================ */
(function () {
  'use strict';

  window.PepperLib = window.PepperLib || {};

  // Help overlay is managed directly in app.js via
  // the #btn-help and #btn-help-dismiss buttons.
  // No screen registration needed since it's an overlay,
  // not a state machine screen.
})();
